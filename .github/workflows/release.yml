# Build gitui installers for macOS (Apple Silicon + Intel), Linux and Windows.
#
# Triggers on a version tag (e.g. `git tag v0.1.0 && git push --tags`) or manually from the
# Actions tab. Produces a *draft* GitHub Release with the per-platform bundles attached:
#   - macOS:   .dmg / .app           (aarch64 + x86_64)
#   - Linux:   .AppImage / .deb / .rpm
#   - Arch:    .pkg.tar.zst          (EndeavourOS & Arch-based, native pacman package)
#   - Windows: .msi / .exe (NSIS)
# Review the draft, then publish it.
name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

# Least privilege by default; the build job below opts into `contents: write` for the
# release upload.
permissions:
  contents: read

jobs:
  # Pre-create the draft GitHub Release, on purpose WITHOUT a target_commitish.
  #
  # tauri-action's own release creation always sends `target_commitish=<the tag's commit>`
  # (see its create-release.ts). When that commit touches `.github/workflows/`, GitHub then
  # also requires the `workflows: write` permission — which GITHUB_TOKEN can NEVER be granted
  # — and answers 403 "Resource not accessible by integration". Creating the release here with
  # `gh` (which sends no target_commitish) avoids that check; tauri-action then simply finds
  # this draft by tag and uploads the bundles to it instead of creating it itself.
  create-release:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Create the draft release if it does not exist yet
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.ref_name }}
        run: |
          if gh api "repos/$GITHUB_REPOSITORY/releases" --jq '.[].tag_name' | grep -qx "$TAG"; then
            echo "Release for $TAG already exists — reusing it."
          else
            # No default body on purpose: the in-app "what's new" dialog shows the release body
            # verbatim, so we leave it empty and you write the user-facing notes when publishing.
            # No notes → empty body → no popup (instead of boilerplate). Re-runs reuse the existing
            # release, so your notes are never clobbered.
            gh api -X POST "repos/$GITHUB_REPOSITORY/releases" \
              -f tag_name="$TAG" \
              -f name="gitui $TAG" \
              -F draft=true >/dev/null
            echo "Created draft release for $TAG (empty body — add your notes before publishing)."
          fi

  build:
    needs: create-release
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest # Apple Silicon
            args: --target aarch64-apple-darwin
          - platform: macos-latest # Intel
            args: --target x86_64-apple-darwin
          - platform: ubuntu-22.04
            args: ""
          - platform: windows-latest
            args: ""

    runs-on: ${{ matrix.platform }}
    steps:
      # All third-party actions are pinned to a full commit SHA (supply-chain hardening);
      # the trailing comment is the human-readable tag. Dependabot (.github/dependabot.yml)
      # keeps these current.
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      # Tauri v2 system libraries (Linux only).
      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: lts/*

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # stable branch, pinned
        with:
          # Both macOS arches need their target installed; harmless elsewhere.
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Cache Rust build
        uses: swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          workspaces: ./src-tauri -> target

      - name: Install frontend dependencies
        run: npm install

      # Derive the bundle version from the tag (v0.2.3 -> 0.2.3) so a plain
      # `git tag && git push` is enough — no manual version bump. No-op off a tag.
      - name: Stamp version from the tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: node .github/scripts/stamp-version.mjs

      - name: Build & bundle
        uses: tauri-apps/tauri-action@84b9d35b5fc46c1e45415bdb6144030364f7ebc5 # v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Sign the updater artifacts and emit latest.json (see plugins.updater in tauri.conf.json).
          # If these secrets are unset, tauri-action just skips signing — the build still works.
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "gitui v__VERSION__"
          releaseBody: "Téléchargez l'installateur correspondant à votre système ci-dessous."
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

  # EndeavourOS / Arch Linux native package (.pkg.tar.zst).
  #
  # Arch can't install the .deb/.rpm produced above, so we build a real pacman package
  # inside an Arch container: compile the release binary, then wrap the prebuilt artifacts
  # with packaging/arch/PKGBUILD via makepkg (which refuses to run as root — hence the
  # throwaway `builder` user). `needs: create-release` guarantees the draft release exists
  # (and decouples this from the matrix, so a flaky macOS DMG bundle can't block it); we just
  # attach the package to it with `gh`.
  arch:
    needs: create-release
    permissions:
      contents: write
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest
    steps:
      # git must be present before checkout so it clones (vs. the tarball fallback);
      # rust/nodejs/npm + the Tauri v2 system libs cover the actual build.
      - name: Install Arch build dependencies
        run: |
          pacman -Syu --noconfirm --needed \
            base-devel git rust nodejs npm \
            webkit2gtk-4.1 gtk3 librsvg openssl \
            github-cli

      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Install frontend dependencies
        run: npm install

      # Same tag-derived version as the build job, so the .pkg.tar.zst pkgver matches.
      - name: Stamp version from the tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: node .github/scripts/stamp-version.mjs

      # `--no-bundle`: we only need the release binary (src-tauri/target/release/gitui);
      # makepkg does the Arch packaging below. Avoids pulling in AppImage/deb/rpm tooling.
      - name: Build binary
        run: npx tauri build --no-bundle

      - name: Build pacman package
        run: |
          set -eux
          version="$(node -p "require('./src-tauri/tauri.conf.json').version")"
          workdir="$(mktemp -d)"

          # Stage PKGBUILD + the prebuilt artifacts it installs ($startdir in package()).
          cp packaging/arch/PKGBUILD packaging/arch/gitui.desktop "$workdir/"
          cp src-tauri/target/release/gitui "$workdir/gitui"
          cp -r src-tauri/icons "$workdir/icons"

          sed -i "s|^pkgver=.*|pkgver=${version}|" "$workdir/PKGBUILD"
          sed -i "s|^url=.*|url=\"${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}\"|" "$workdir/PKGBUILD"

          # makepkg won't run as root; build the package as an unprivileged user.
          # --nodeps: runtime depends() aren't needed just to wrap prebuilt files.
          useradd -m -s /bin/bash builder
          chown -R builder:builder "$workdir"
          runuser -u builder -- bash -c "cd '$workdir' && makepkg -f --nodeps"

          mkdir -p arch-dist
          cp "$workdir"/*.pkg.tar.zst arch-dist/

      - name: Attach package to the draft release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "$GITHUB_REF_NAME" arch-dist/*.pkg.tar.zst \
            --clobber -R "$GITHUB_REPOSITORY"
